name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # Install uv (fast Python + package manager) and enable caching
      - name: Setup uv
        uses: astral-sh/setup-uv@v7

      # Cache uv tool + resolver cache
      - name: Cache uv caches
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/uv
          key: uv-cache-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'uv.lock') }}

      - name: Run shared lint policy
        run: uv run --no-project python scripts/run_ci_locally.py lint

  typecheck-and-test:
    name: typecheck-and-test (py${{ matrix.py }})
    runs-on: ubuntu-latest
    needs: [lint]
    strategy:
      fail-fast: false
      matrix:
        py: ["3.11", "3.12", "3.13"]
    env:
      S2AND_TEST_REQUIRE_RUST: "1"
    steps:
      - uses: actions/checkout@v5
        with:
          lfs: true

      - name: Setup uv
        uses: astral-sh/setup-uv@v7

      # Optional: ensure a specific Python (uv can also manage this on its own)
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.py }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo build
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            s2and_rust/target
          key: cargo-${{ runner.os }}-py${{ matrix.py }}-${{ hashFiles('s2and_rust/**', '!s2and_rust/target/**', '!s2and_rust/dist/**') }}
          restore-keys: |
            cargo-${{ runner.os }}-py${{ matrix.py }}-

      # Cache uv resolver + wheels + project venv
      - name: Cache uv + venv
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-py${{ matrix.py }}-${{ hashFiles('pyproject.toml', 'uv.lock', 's2and_rust/**', '!s2and_rust/target/**', '!s2and_rust/dist/**') }}

      - name: Run shared typecheck-and-test policy
        run: uv run --no-project python scripts/run_ci_locally.py typecheck-and-test
