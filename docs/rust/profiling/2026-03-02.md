# Profiling Findings (2026-03-02, gate rerun refresh)

Date: 2026-03-02

## Scope

Reran the three active promotion gates on today’s code:
1. Comparator (`inspire`, 5k/5k)
2. Transfer-mini full (`kisti`, preset full)
3. Stress rebuild (`aminer`, `from_json_paths`, 6 repeats)

Build:
- `uv run maturin develop -m s2and_rust/Cargo.toml --release`

At run time, a temporary subprocess import pre-step was used:
- `$env:PYTHONPATH='scripts'`
- Follow-up fix landed: `_rust_suite` entry scripts now self-bootstrap `scripts/` on `sys.path`, so this pre-step is no longer required.

## Artifacts

- `scratch/baselines_20260302/compare_inspire_5k_20260302.json`
- `scratch/baselines_20260302/profile_transfer_mini_full_20260302.json`
- `scratch/baselines_20260302/stress_rust_from_json_paths_aminer_6x_20260302.json`

## Results (source of truth: JSON)

See the JSON artifacts above for timing/RSS/quality deltas and run metadata (including `workload_id` and `git_dirty`).
